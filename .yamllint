# Relaxed base: enforcing document starts or 80-column lines on the workflows and the
# compose files of this repository buys nothing.
extends: relaxed

rules:
  line-length:
    max: 120
  # Line endings must stay LF: a CRLF compose file breaks tooling on Linux.
  new-lines:
    type: unix
