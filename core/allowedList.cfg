# UNUSED / LEGACY: no run-phase or prune-phase code reads this file. The network
# allow-list Phobos enforces is the [connect] section of a policy cfg, turned
# into Landlock --connect-tcp ports and the libnetblocker NETBLOCKER_CONF rules
# file (host<space>port), not this colon-separated list. It is kept only because
# docker/run_phase/java/Dockerfile still copies it; safe to remove together with
# that COPY and the assemble-run-phase-context.sh line.
127.0.0.1:*
::1:*
localhost
